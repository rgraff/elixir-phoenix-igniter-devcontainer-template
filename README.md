# elixir-phoenix-igniter-devcontainer-template
An example for setting up a devcontainer with elixir, phx-new, and igniter_new

Use this template to initialize a new repo. Open the repo as a devcontainer and follow
the prompts to initialize a new Phoenix app with Ash (or without).
